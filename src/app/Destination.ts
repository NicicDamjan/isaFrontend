export class Destination {
    public id: number;
    public name: string;
    public nick: string;
    public state: string;
    public city: string;
  
    // constructor(id: number, name: string, nick: string){}
  }